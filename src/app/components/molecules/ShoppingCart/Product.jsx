

/*const Product = ({ data, addToCart }) => {
  const { id, name, price, imgUrl } = data;
  return (
    <div className="product flex flex-col items-center justify-center mb-2">
      <h4>{name}</h4>
      <img className="w-24 h-15" src={imgUrl} alt="" />
      <h5>$ {price}</h5>
      <button className="bg-[#3656b8] rounded-full px-4 py-1 text-custom-white font-semibold hover:bg-[#547cf5]" onClick={() => addToCart(id)}>Agregar</button>
    </div>
  );
};

export default Product;*/
