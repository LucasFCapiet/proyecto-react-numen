/*const CartItem = ({ data, deleteFromCart }) => {
  let { id, name, price, quantity , imgUrl } = data;
  return (
    <div className="cart-item flex flex-col items-center justify-center mb-2 ">
      <h4>{name}</h4>
      <img className="w-24 h-15" src={imgUrl} alt="" />
      <h5>
        $ {price} x {quantity} = ${price * quantity}
      </h5>
      <div><button className="bg-[#A52A2A] rounded-full px-4 py-1 text-custom-white font-semibold hover:bg-[#DC143C]" onClick={() => deleteFromCart(id)}>Eliminar uno</button>
      <button className="bg-[#A52A2A] rounded-full px-4 py-1 text-custom-white font-semibold hover:bg-[#DC143C]"onClick={() => deleteFromCart(id, true)}>Eliminar todos</button>
</div>
          </div>
  );
};

export default CartItem;*/
